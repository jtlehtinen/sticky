<Window x:Class="Sticky.NoteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sticky"
        mc:Ignorable="d"
        AllowsTransparency="True"
        Height="200"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False"
        SizeChanged="OnSizeChanged"
        Title="sticky"
        Topmost="True"
        Width="306"
        WindowStyle="None">

  <Window.Resources>
  </Window.Resources>

  <DockPanel Background="{DynamicResource Background}">
    <local:NoteWindowTitleBar x:Name="TitleBar" Margin="0,0,0,10" VerticalAlignment="Top" DockPanel.Dock="Top">
      <local:NoteWindowTitleBar.RenderTransform>
        <TranslateTransform Y="-42"/>
      </local:NoteWindowTitleBar.RenderTransform>
    </local:NoteWindowTitleBar>

    <Border DockPanel.Dock="Bottom" BorderThickness="0 1 0 0" BorderBrush="#10000000">
      <StackPanel  Orientation="Horizontal" Margin="0,0,0,0">
        <StackPanel.Resources>
          <local:BindingProxy x:Key="BackgroundProxy" Data="{DynamicResource Background}"/>

          <!-- Footer Button -->
          <Style
            BasedOn="{StaticResource TitleBarButton}"
            TargetType="{x:Type Button}"
            x:Key="FooterButton">

            <Setter Property="Foreground" Value="{DynamicResource ForegroundSecondary}"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="5,5,5,5"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Template">
              <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                  <Border
                    Background="{TemplateBinding Background}"
                    BorderBrush="Transparent"
                    BorderThickness="0"
                    SnapsToDevicePixels="True">
                    <ContentPresenter
                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                    />
                  </Border>
                  <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="true">
                      <Setter
                        Property="Background"
                        Value="{Binding Source={StaticResource BackgroundProxy}, Path=Data, Converter={StaticResource BackgroundToHoverConverter}}"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="true">
                      <Setter
                        Property="Background"
                        Value="{Binding Source={StaticResource BackgroundProxy}, Path=Data, Converter={StaticResource BackgroundToPressedConverter}}"/>
                    </Trigger>
                  </ControlTemplate.Triggers>
                </ControlTemplate>
              </Setter.Value>
            </Setter>
          </Style>
        </StackPanel.Resources>

        <Button Style="{StaticResource FooterButton}" Content="&#xE8DD;" FontSize="16" ToolTip="Bold"/>
        <Button Style="{StaticResource FooterButton}" Content="&#xE8DB;" FontSize="16" ToolTip="Italic"/>
        <Button Style="{StaticResource FooterButton}" Content="&#xE8DC;" FontSize="16" ToolTip="Underline"/>
        <Button Style="{StaticResource FooterButton}" Content="&#xEDE0;" FontSize="18" ToolTip="Strikethrough"/>
        <Button Style="{StaticResource FooterButton}" Content="&#xE8FD;" FontSize="18" ToolTip="Toggle Bullets"/>
        <Button Style="{StaticResource FooterButton}" Content="&#xEB9F;" FontSize="18" ToolTip="Add Image"/>
      </StackPanel>
    </Border>

    <Grid>
     <TextBlock
      FontSize="14"
      Foreground="{DynamicResource ForegroundSecondary}"
      IsHitTestVisible="False"
      Margin="5,0"
      MinWidth="50"
      Name="Placeholder"
      Text="Take a note..."/>

      <RichTextBox
        Background="Transparent"
        BorderBrush="Transparent"
        BorderThickness="0"
        FontSize="14"
        Foreground="{DynamicResource Foreground}"
        HorizontalScrollBarVisibility="Auto"
        Name="NoteInput"
        TextChanged="OnTextChanged"
        VerticalScrollBarVisibility="Auto">

        <RichTextBox.Resources>
          <Style TargetType="{x:Type Paragraph}">
            <Setter Property="Margin" Value="0"/>
          </Style>
        </RichTextBox.Resources>
      </RichTextBox>
    </Grid>
  </DockPanel>
</Window>
