<Window x:Class="Sticky.NoteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sticky"
        mc:Ignorable="d"
        Title="sticky"
        Height="200"
        Width="306"
        WindowStyle="None"
        AllowsTransparency="True"
        SizeChanged="OnSizeChanged"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False"
        Topmost="True">

  <Window.Resources>
  </Window.Resources>

  <DockPanel Background="#FFE2F1FF">
    <local:NoteWindowTitleBar x:Name="TitleBar" Margin="0,0,0,0" VerticalAlignment="Top" DockPanel.Dock="Top">
      <local:NoteWindowTitleBar.RenderTransform>
        <TranslateTransform Y="-42"/>
      </local:NoteWindowTitleBar.RenderTransform>
    </local:NoteWindowTitleBar>
    <RichTextBox Name="NoteInput" Background="#00FFFFFF" BorderBrush="Transparent" BorderThickness="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontSize="14">
      <RichTextBox.Resources>
        <Style TargetType="{x:Type Paragraph}">
          <Setter Property="Margin" Value="0"/>
        </Style>
      </RichTextBox.Resources>
    </RichTextBox>
  </DockPanel>
</Window>
