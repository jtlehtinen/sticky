<UserControl
  x:Class="Sticky.SettingsPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:ui="http://schemas.modernwpf.com/2019"
  xmlns:local="clr-namespace:Sticky"
>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!-- Title Bar -->
    <local:SettingsPageTitleBar Grid.Row="0" Margin="0"/>

    <ui:SimpleStackPanel
      Grid.Row="1"
      Orientation="Vertical"
      Margin="10,0,10,10"
      Spacing="10"
    >
      <!-- Heading -->
      <TextBlock
        Style="{StaticResource SubtitleTextBlockStyle}"
        Text="Settings"
        FontWeight="Bold"
      />

      <!-- General Settings -->
      <Separator/>
      <TextBlock
        Style="{StaticResource SubtitleTextBlockStyle}"
        Text="General"
        FontWeight="Bold"
      />

      <ui:ToggleSwitch
        Header="Confirm before deleting"
        IsEnabled="True"
        IsOn="{Binding Path=ConfirmBeforeDelete, Mode=TwoWay}"
        OffContent="Off"
        OnContent="On"
      />

      <ui:ToggleSwitch
        Header="Pin new note"
        IsEnabled="True"
        IsOn="{Binding Path=PinNewNote, Mode=TwoWay}"
        OffContent="Off"
        OnContent="On"
      />

      <!-- Color Settings -->
      <Separator/>
      <TextBlock
        Style="{StaticResource SubtitleTextBlockStyle}"
        Text="Color"
        FontWeight="Bold"
      />

      <ui:RadioButtons x:Name="ThemeOptions" SelectedIndex="0">
        <RadioButton x:Name="RadioButtonThemeLight" Content="Light" Command="{x:Static local:Commands.ChangeAppTheme}" CommandParameter="Light"/>
        <RadioButton x:Name="RadioButtonThemeDark" Content="Dark" Command="{x:Static local:Commands.ChangeAppTheme}" CommandParameter="Dark"/>
        <RadioButton x:Name="RadioButtonThemeSystem" Content="Use system setting" Command="{x:Static local:Commands.ChangeAppTheme}" CommandParameter="System"/>
      </ui:RadioButtons>

      <!-- About -->
      <Separator/>
      <TextBlock
        Style="{StaticResource SubtitleTextBlockStyle}"
        Text="About"
        FontWeight="Bold"
      />

      <TextBlock
        Style="{StaticResource CaptionTextBlockStyle}"
        Text="Sticky 0.0.1"
        FontWeight="Bold"
      />
      <Button
        Content="Export Notes"
        Click="OnExportNotes"
      />

    </ui:SimpleStackPanel>
  </Grid>
</UserControl>
